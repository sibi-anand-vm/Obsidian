1.Lets make  home.html as Record view page if user is authenticated:
```
{% if user.is_authenticated %}
{% if files %}
<h1>Customer Files</h1>
{% for f in files %}
{{ f }}
{% endfor %}
{% endif %}
{% else %}
```
![[Pasted image 20241224181438.png]]
2.Send all files in **home func** in views.py by below lines(Below code not serialised).Refer pic for more info:
```
import .models import File
files=File.objects.all()
return render(request,'home.html',{'files':files})
```
![[Pasted image 20241224182029.png]]

3.The output will be like below:
![[Pasted image 20241224181706.png]]