## ###<mark style="background: #FFB86CA6;"> Explanation:</mark>

1. **If-Else**: Checks if the person is eligible to vote based on age.
2. **Else-If**: Classifies the person as a minor, adult, or senior citizen based on age.
3. **Nested If**: Differentiates young adults and regular adults, using a nested `if` inside the main `if` condition.
4. **Switch**: Checks the value of the `day` variable and prints a message based on the day, using `break` to prevent falling through cases.
```

```