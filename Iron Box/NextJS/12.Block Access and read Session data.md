1.Modify code in <font color="#4bacc6">middleware.ts </font>to block access without Signing in.
```
import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";

const isProtectedRoute = createRouteMatcher(["/mock-users"]);
export default clerkMiddleware(async (authPromise, req) => {

const auth = await authPromise;
if (isProtectedRoute(req)) {
await auth.protect();
}
});

export const config = {
matcher: [
"/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)",
"/(api|trpc)(.*)",
],
};
```

2.Also add some lines to read session data in page.tsx of <font color="#4bacc6">mock-users</font> folder.(Not full code is given)
```
import {auth,currentUser} from "@clerk/nextjs/server"
let authobj=auth();
let currentuserobj=currentUser();
console.log({authobj,currentuserobj})
```
