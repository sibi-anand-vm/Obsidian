1.Lets set up Loading status and Error handling by creating <font color="#4bacc6">error.ts and loading.ts</font> file in <font color="#4bacc6">users-client</font> folder.
![[Pasted image 20250203192337.png]]
2.Copy paste below code in loading.ts
```
export default function Loading(){
return <div>Loading...</div>
}
```
3.Copy paste below code in error.ts
```
"use client"
import { useEffect } from "react";

export default function Error({message}:{message:String}){
useEffect(()=>{
console.log(message);
},[message])

return(
<div>Error fetching Users data</div>
);
}
```

Next JS automatically detects loading and error handling by its own.

4.Modify page.tsx for detecting loading and error handling.
```
"use client"

import { useState,useEffect } from "react"
import Loading from "./loading";
import Error from "./error";

export default function UsersFetch(){
type User = {
id: number;
name: string;
email: string;
};

const [users,setusers]=useState<User[]>([]);
const [loading,setloading]=useState(true);
const [error,seterror]=useState('');

useEffect(()=>{
async function fetchusers(){

try{
await new Promise((resolve)=>setTimeout((resolve),2000));
let response=await fetch('https://jsonplaceholder.typicode.com/users');
if(!response.ok) {
seterror("Failed to fetch data from URL");
}
let data=await response.json();
setusers(data);
console.log("Data downloaded")
}
catch(err){
seterror("Failed to fetch data");
}
finally{
setloading(false);
}
}

fetchusers();
},[])

if(loading) return <Loading />
if(error) return <Error message={error} />
return(
<>
{users.map((user)=>(
<div key={user.id}>
<h1>{user.name}</h1>
</div>
))}
</>
);
}
```

Key Modification:
Added error,loading states for auto handling and Promise.
```
const [loading,setloading]=useState(true);
const [error,seterror]=useState('');

await new Promise((resolve)=>setTimeout((resolve),2000));

if(loading) return <Loading />
if(error) return <Error message={error} />
```

![[Pasted image 20250204072118.png]]
